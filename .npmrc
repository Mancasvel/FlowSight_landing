# Prefer patch-level updates on `pnpm add` (same minor line).
save-prefix=~
engine-strict=true
# Hoist types + Next so tooling resolves like a flat install where needed.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=@types/*
public-hoist-pattern[]=next
